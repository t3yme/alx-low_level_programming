#include "main.h"
/* more headers goes there */

/**
 * add - Defines the function to add two integers and returns the result
 * @a: first parameter provided for testing
 * @b: second parameter provided for testing
 * Return: evaluates to integer on success
 */
int add(int a, int b)
{
	return (a + b);
}
