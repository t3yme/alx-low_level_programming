#include "main.h"
/* more headers goes there */

/**
 * _abs - Defines the function to compute the absolute value of an integer.
 * @n: parameter provided for testing
 * Return: evaluates to integer on success
 */

int _abs(int n)
{
	if (n > 0)
		return (n);

	return (-n);
}
